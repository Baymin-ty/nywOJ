# LibreOJ Feature Parity

Reference repositories checked on 2026-06-30:

- https://github.com/LibreOJ/backend
- https://github.com/LibreOJ/frontend
- https://github.com/LibreOJ/judge

## Implemented or Already Covered

- API surface audit: all 88 upstream LibreOJ backend controller routes are registered locally with exact `/api/*` paths as of the 2026-06-30 check against backend `f477a3d962117bc587e3b61f61e2f01df1f9828c`.
- User registration and admin rename flows with upstream username rules, username/email availability checks, LibreOJ-compatible `/api/auth/*` account/session endpoints, login/register/reset-password/email-verification enum errors, `USER_NOT_MIGRATED` login handoff into the user-migration flow, upstream email-code rate limiting/TTL and optional register-time verification according to security preferences, reset-password handling for non-migrated accounts that updates the migration old-password hash, token-aware `getSessionInfo` JSON/JSONP responses with upstream-shaped self `userMeta` and `UserPrivilegeType[]`, verified self-email updates with upstream verification-before-duplicate-check ordering plus profile-email changes and username changes gated by the same security preferences, upstream-shaped session list/revoke responses with numeric `sessionId` values and old token fallback, complete client-facing sessionInfo server preferences for security/pagination/misc defaults, username profile routes/search/meta lookup with wildcard search and privilege lookup semantics, enum-validated privilege writes with local permission-catalog protection, upstream-shaped user list pagination, `sortUserByRating` default sorting, and `TAKE_TOO_MANY` handling, extended profile/social fields, Gravatar/QQ/GitHub avatars, admin-validated username/email updates, user ranklist/rank metadata, contest participation stats, tie-aware AC and Rating leaderboards, and audit logs with configured query-limit handling plus manager all-user/object-id filters.
- RBAC-style permissions, scoped problem/contest collaborators and admin role management.
- User groups with group admins, member management, group-inherited permissions and LibreOJ-compatible group meta/search/list GET endpoints, including upstream 1-48 character group-name rules, public group meta search with configured `searchGroup` limits, upstream wildcard matching, enum errors, privacy-safe member user metadata and official member-list response shape.
- Problem CRUD, deletion cleanup, visibility, pid-based problem navigation, statement samples, admin-managed tag catalog with colors, stats, test data management and solution paste binding.
- Frontend route compatibility for current LibreOJ paths and common old redirects, without hard-coded deployment-host redirects and with encoded username-character-aware public profile links/access: `/login`, `/register`, `/forgot`, `/homepage-settings`, `/p`, `/p/new`, `/p/id/:pid`, `/p/:pid/(edit|files|judge-settings|statistics/:type)`, `/s`, `/u`, `/user/:uid -> /u/:username`, `/user/:uid/edit/:type`, `/d`, `/d/new`, `/d/:did/edit`, `/judge-machine`, `/ranklist`, `/article/:did`, `/problems/tag/:ids` and related `/problem/:pid/*` redirects now land on the local Vue pages or bridge flows.
- LibreOJ-compatible `/api/problem/*` problem creation/query with official `statement` payloads and `id` responses, `titleOnly`, tag/owner/nonpublic filters, privacy-safe owner/filter-owner user metadata, configured `problemSet` query limits and upstream-style missing-owner fallback, problem-tag CRUD response/error shapes, publicness/delete enum responses with upstream permission semantics, chunked problem detail, statement update, upstream-style current-user permission semantics, collaborator permissions with expired-grant filtering and permission-catalog-safe writes, problem-file add/remove/rename/download, judge-info update and problem-type change endpoints.
- Problem asset file APIs: online save/delete/rename/download for checker, interactor, grader and support files.
- Traditional, SPJ, submit-answer and custom-profile judging flows.
- Wider language matrix: C, C++, Python 3, Java, Kotlin, Pascal, Rust, Go, Swift, Haskell, C# and F# profiles.
- Submission list/detail, logs, rejudge, pending-only stale-result-clearing cancel, upstream-style visibility/action permissions including record-management-aligned publicness/delete controls, privacy-safe submitter metadata with effective Rating and real activity counts, upstream-style cancel permissions/no-op finished cancel, filtered ID-cursor pagination, configured submission/statistics query limits, live progress streaming and LibreOJ-style per-problem top-100 statistics leaderboards without the old 50-row page cap, with LibreOJ-compatible `/api/submission/*` lifecycle endpoints.
- LibreOJ-compatible Socket.IO `/api/socket` `submission-progress` bridge backed by local judging events; compatible submission APIs now return `progressSubscriptionKey` for pending list/detail subscriptions.
- Submission public/private toggles, deletion and downloadable submission files with LibreOJ-requested filenames.
- Contest creation, registration, participant management, OI/IOI ranking and stale-case-clearing/stat-refreshing rejudge with problem-vs-contest permission boundaries and finished-contest manager UI access, plus configurable rated/unrated contests with list/detail/rank/paginated-rating-change settlement-status visibility, sparse-id-safe rank construction for Rating calculation, Elo-style contest rating settlement for users who actually submitted during the contest, active-timeline-safe auto-settlement, pending-judgement-safe settlement/rebuild guards that stop chronological rebuilds at the first unfinished rated contest while allowing sample-insufficient contests to be skipped without blocking, conflict-aware manager/admin rating preview with first-mismatch drift details and capped large-contest preview payloads, direct admin settlement for pending rated contests, active-only per-contest and paginated user rating history with duplicate-safe public change/history display, conservative manager-triggered recalculation with visible backend failure reasons, serialized Rating write operations whose first-settle/recalculate paths resync user caches from effective history, history-derived base ratings that do not trust mutable user caches, effective-history-based local and LibreOJ-compatible user Rating ranklists with cache fallback that excludes deleted users from latest-history calculations via one shared latest-history/storage-schema SQL helper with old-table missing-key-column/column/index/order-insensitive-primary-key-or-unique/nullability and concurrency-safe column-aware helper-index self-healing and duplicate-history-safe latest-rating selection, existing-user-and-valid-last-submission-only submitted/pending-judgement counts through shared SQL helpers so deleted-user or orphan last-submission remnants cannot keep contests pending or blocked, admin-visible invalid-last-submission counts/reason drilldowns plus single-contest status/preview invalid-count hints for rated contests, public/admin pending-judgement status with affected-user/problem counts, user-history current-rating verification plus profile/user-list cache-mismatch visibility, visible Rating policy parameters, explicit sample-insufficient rebuild reasons, and rebuild/blocker payloads plus failure messages that carry pending-user/problem and invalid-last-submission diagnostics, dry-run and write-mode pollution-safe full chronological admin rebuild that leaves stored history untouched when a pending-judgement blocker is found and reports planned-vs-written counts/status, active/stale/null-key/duplicate/pending/judging/sample-insufficient/drifted rating admin stats with concrete drilldowns and live primary-key/unique-constraint/helper-index status, adjustable admin drilldown/rebuild result sample size with explicit omitted-result metadata, active/disabled user breakdowns and stale/cache-drift reason breakdowns that show displayed-vs-total counts plus stale-after-rejudge recalculation workflow, chronological dry-run drift auditing for both standing changes and old-rating timeline contamination, single-settlement/cleanup/rebuild storage recovery for invalid contest/user, null-key and duplicate Rating rows that always follows with cache resync and reports final primary-key/unique recovery status plus blockers and alternate unique-key names when old indexes occupy the preferred name, cache drift detection and temp-table-backed differential sync with before/after mismatch counts, rank-table display, rating tiers, user profile rating trend charts.
- Paste, announcements, custom run, online IDE, judge monitor, CORS/StreamSaver helper endpoints including LibreOJ `/api/cors/*` aliases with official `security.crossOrigin` config support, Prometheus-style metrics export and optional Telegram event reporting for uncaught backend errors.
- LibreOJ-compatible cron maintenance endpoint `POST /api/runMaintainceTasks` with `maintaince-key` authentication and local temporary upload cleanup.
- Judge client lifecycle: add/edit/delete/enable clients, reset client keys, LibreOJ-compatible `/api/judgeClient/*` management endpoints with upstream admin-only key/`allowedHosts` visibility, official enum responses for missing clients, session-like online status from active sockets/recent non-offline heartbeats, socket-time source host enforcement, authenticated task push and heartbeat/queue telemetry.
- LibreOJ judge client Socket.IO `/api/socket` `judge` namespace: official clients can authenticate with the generated 40-character Base64 key, receive `ready`, send `systemInfo`, download signed test-data files, consume converted Traditional/SPJ/SubmitAnswer tasks plus LibreOJ-native Interaction `judgeInfo`, and report progress back into local submission tables. Local extension profiles that cannot be represented by the official client, such as communication/function tasks or answer-aware pipe groups, fall back to the local worker.
- Discussion list/detail/edit/reply/reaction flow with visibility, LibreOJ-compatible `problemId`/`publisherId` list filters (global/problem/all-problems semantics plus `filterProblem`/`filterPublisher` response fields), `titleOnly` search previews, privacy-safe publisher/reply user metadata with effective Rating and real activity counts, upstream-shaped create/query/detail/reply/update/delete/publicness endpoints with official enum error and permission-array semantics, upstream create-discussion security/default-public preferences, and discussion/reply `TAKE_TOO_MANY` query-limit handling.
- Local and S3-compatible problem-file storage abstraction with signed upload/download URLs and remote judge data archive restore.
- Migration export/import tools for users, problems, problem tags, submissions and discussions, with dry-run/upsert import and LibreOJ-compatible user-migration login flow, including upstream 3-24 character username rules for forced rename and official enum-only migration password-update responses.
- First-class interactive/communication problem UI with pipe-group setup shortcuts and submission result summaries.
- Homepage settings editor with configurable notice, discussion-backed announcements, hitokoto, countdown, problem search, friend links, LibreOJ top-users/latest-problems with public-profile-safe user payloads and effective-history Rating display/sorting, rabbit stats/rank and custom Markdown blocks, plus LibreOJ-compatible `/api/homepage/*` content/settings endpoints backed by the same `home.config`, including all-locale announcement discussion metadata for settings, homepage user/problem list pagination and user-rank sorting preferences.

## Remaining LibreOJ Parity Work

- No route-level gaps were found in the latest upstream audit. Remaining risk is manual/runtime behavior parity for edge cases not covered by automated checks.
